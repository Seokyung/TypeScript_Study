import { hello } from './hello';

console.log('Hello TypeScript!');
const a = 1; // js에서는 타입 관련 코드들이 삭제됨

// compilerOptions - target
const func = () => console.log('arrow function');

// compilerOptions - module
hello();